// Fuzzer regression tests removed.
// The fuzzer's generator and seed are the committed artifacts; generated models are ephemeral.
// When a real failure is found, commit one minimal reproducer as a named fixture
// (tests/fixtures/regression/<short-bug-name>.bngl) with a targeted test.
// Do NOT re-create a suite that loops over bulk-committed random models.
